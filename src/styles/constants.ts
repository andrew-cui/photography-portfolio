export const navHeightHeader = Number(
  getComputedStyle(document.documentElement)
    .getPropertyValue("--nav-height-header")
);

export const navRadius = Number(
  getComputedStyle(document.documentElement)
    .getPropertyValue("--nav-radius")
);